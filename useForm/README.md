# useForm

Usage Example:

```
    const initialForm = {
        name: '',
        age: 0,
        email: ''
    };
    
    const [ formValues: null, handleInputChange: void, reset: void ] = useForm( initialForm );

```